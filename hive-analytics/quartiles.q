-- 25th percentile (1st quartile)
SELECT PERCENTILE(a, 0.25) FROM test;

-- 50th percentile (2nd quartile)
SELECT PERCENTILE(a, 0.50) FROM test;

-- 75th percentile (3rd quartile)
SELECT PERCENTILE(a, 0.75) FROM test;
